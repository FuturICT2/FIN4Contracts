pragma solidity ^0.5.17;

contract MintTransferStub {

    function mint(address account, uint256 amount) public returns (bool);

    function transfer(address recipient, uint256 amount) public returns (bool);

}
